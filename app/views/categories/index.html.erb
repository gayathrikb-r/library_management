<h1>📚 Categories</h1>

<% if librarian? %>
  <%= link_to "➕ Add New Category", new_category_path, class: "btn btn-success" %>
<% end %>

<div class="grid">
  <% @categories.each do |category| %>
    <div class="card">
      <h3><%= link_to category.name, category %></h3>
      <p><strong>Books:</strong> <%= category.books_count %></p>
      
      <% if librarian? %>
        <div style="margin-top: 10px;">
          <%= link_to "Edit", edit_category_path(category), class: "btn" %>
          <%= link_to "Delete", category, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, 
              class: "btn btn-danger" %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>