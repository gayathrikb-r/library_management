<h1>Categories</h1>

<div data-controller="categories">
  <% if librarian_signed_in? %>
    <%= link_to "➕ Add New Category", new_category_path, class: "btn btn-success mb-20" %>
  <% end %>

  <div class="grid" data-categories-target="grid">
    <!-- Categories will be loaded here via JavaScript -->
  </div>

  <!-- ADD THIS LINE: -->
  <div data-categories-target="pagination"></div>
</div>

