<div 
  data-controller="category-show" 
  data-category-show-category-id-value="<%= @category.id %>"
  data-librarian-signed-in="<%= librarian_signed_in? %>">
  
  <div class="card" data-category-show-target="categoryDetails">
    <!-- Category details will be loaded here -->
  </div>

  <div class="card" data-category-show-target="booksList">
    <!-- Books will be loaded here -->
  </div>
</div>