<div class="card" 
     data-controller="reservation-show" 
     data-reservation-show-reservation-id-value="<%= @reservation.id %>">
  
  <div data-reservation-show-target="details">
    <!-- Reservation details will be loaded here -->
  </div>
</div>
